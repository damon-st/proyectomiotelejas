// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  // For Firebase JS SDK v7.20.0 and later, measurementId is optional
  // For Firebase JS SDK v7.20.0 and later, measurementId is optional
 firebaseConfig : {
  apiKey: 'AIzaSyCGcJYuzjPixmlWT-PriKS3uztQktlh8Is',
  authDomain: 'telejaswebapp.firebaseapp.com',
  databaseURL: 'https://telejaswebapp.firebaseio.com',
  projectId: 'telejaswebapp',
  storageBucket: 'telejaswebapp.appspot.com',
  messagingSenderId: '1097716891707',
  appId: '1:1097716891707:web:51750ed33eafcbcb2d5c1e',
  measurementId: 'G-7HVKPEGLZN'
}
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
